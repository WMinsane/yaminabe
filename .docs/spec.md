# アプリ仕様書

## コンセプト

エコーチェンバーと成長を阻むネット環境からの脱却。
パーソナライズとランダム性を両立し、「何が出てくるかわからない」体験を通じて視野拡大・自己成長・情報収集を兼ねる。

---

## 画面一覧

| # | 画面 | 概要 | アクセス | 認証 | プラン |
|---|------|------|---------|------|--------|
| S-01 | フィード画面 | フィードの閲覧・操作（メイン画面） | メイン（ホーム） | 必須 | 全員 |
| S-02 | ライブラリ画面 | 行動履歴一覧（クリック・ブックマーク・メモ等） | ハンバーガーメニュー | 必須 | 有料 |
| S-03 | 週次ランキング画面 | カテゴリ別の人気記事ランキング閲覧 | ハンバーガーメニュー | 必須 | 有料 |
| S-04 | ユーザー設定画面 | 配信モード・カテゴリ等の設定変更 | ハンバーガーメニュー | 必須 | 全員 |
| S-05 | 認証画面 | ログイン・サインアップ・パスワードリセット申請。未登録ユーザーはトップへのリンクあり | ハンバーガーメニュー / 有料機能アクセス時 | 不要 | 全員 |
| S-06 | パスワード変更画面 | トークン検証後のパスワード再設定 | メールリンク経由 | 不要 | 有料 |
| S-07 | エラー画面 | エラーメッセージの表示（内容は別途定義） | エラー発生時 | — | — |

### ハンバーガーメニュー

ハンバーガーアイコンから固定左サイドパネル（w-56, PC: w-64）を展開。メニューエリア外クリックまたは閉じるボタンで閉じる。

**アイコン配置**: モバイルではヘッダー右端、PCではヘッダー左端（CSS flexbox orderで制御）。ユーザー名はヘッダー右端に表示。

以下への導線を提供:
- ライブラリ（有料のみ: 行動履歴一覧）
- 週次ランキング
- ユーザー設定
- アカウント登録 / ログイン（未認証時）
- ログアウト（認証済み時。ログアウト実行時にメニューも閉じる）

---

## 機能一覧

| # | 機能 | 無料（アカウントなし） | 有料（アカウントあり） | 概要 |
|---|------|:----:|:----:|------|
| F-01 | フィード表示 | ○ | ○ | 日次プールからのフィード表示 |
| F-02 | フィード条件変更 | 1日1回（cookie制限） | 自由 | カテゴリ・配信モードの変更 |
| F-03 | ユーザーアクション | × | ○ | クリック/バウンス/ブックマークの記録（デバウンス方式） |
| F-04 | アクティビティ履歴 | × | ○ | 全行動履歴（クリック・ブックマーク・メモ等） |
| F-05 | リンクメモ | × | ○ | 記事へのメモ（最大1000文字） |
| F-06 | 配信モード選択 | ○ | ○ | 5種の配信モードから選択 |
| F-07 | カテゴリ設定 | ○ | ○ | 興味カテゴリの選択・編集（階層構造） |
| F-08 | 週次ランキング | × | ○ | カテゴリ別の人気記事ランキング |
| F-09 | アカウント管理 | — | ○ | アカウント登録・ログイン・データ永続化 |

---

## F-01: フィード表示

### 概要

日次バッチで収集済みのコンテンツプールから、ユーザーの条件に合致する記事を取得・表示する。
収集処理の詳細は「コンテンツ収集」セクションを参照。

### フィード取得フロー（Server Component）

```
1. ユーザーの選択カテゴリ（user_category）を取得
2. contentテーブルから該当カテゴリの記事を取得（category + contentTags含む）
3. スコアリング: タグ親和度 × 配信モード係数 × 新着ブースト × ブックマーク数で算出
4. 親カテゴリ別にグループ化し、各カテゴリ上位20件を抽出
5. ユーザーアクション（クリック・ブックマーク・メモ）を結合して表示
```

### フィードアイテムの表示情報

| 項目 | 説明 |
|------|------|
| タイトル | 記事タイトル |
| ソース | 配信元（はてブ / note / ブログ名等） |
| カテゴリ | 記事のカテゴリ（階層表示） |
| サマリ | 記事の要約（取得可能な場合） |
| ブックマーク数 | はてブの場合、ブックマーク数を表示 |
| 公開日 | 記事の公開日時 |
| Newバッジ | 最新のcollected_atから24時間以内に収集された記事に表示（バッチ実行日時基準の相対判定） |

---

## F-02: フィード条件変更

### 仕様

条件変更時はサーバーから再取得する。無料ユーザーは条件変更に回数制限あり。

| プラン | 条件変更 | 制限方式 |
|--------|---------|----------|
| 無料（アカウントなし） | 1日1回 | cookie（有効期限: 24時間） |
| 有料（アカウントあり） | 自由 | 制限なし |

- 無料ユーザーが条件変更済みの場合、UIで「次回変更可能: 23:30以降」を表示（日次バッチ直前にリセット）
- cookie期限切れ・クリアで再変更可能（厳密な制御は行わない）
- カテゴリ・配信モードを変えても同一記事が含まれることがある
- 未認証リクエストに対しIPベースのレートリミット（API層）で過剰アクセスを防止（認証済みユーザーは対象外）

---

## F-03: ユーザーアクション

### 概要

ユーザーと記事の組み合わせで1レコード（user_action）。各アクションはbooleanフラグで管理。

| フラグ | トリガー | UI | パーソナライズへの影響 |
|--------|---------|-----|---------------------|
| is_clicked | リンクタップ | — | ポジティブ（該当カテゴリの重み増加） |
| is_bounced | クリック後に早期帰還 | — | ネガティブ（記事は好みでなかった） |
| is_bookmarked | ブックマークボタン（トグル） | ブックマークアイコン | 強ポジティブ |

- アクションなし（user_actionレコードなし）= 興味なしとして扱う（JOINで判定）
- ブックマーク済み記事はライブラリ画面（S-02）のブックマークタブで閲覧（ソート: bookmarked_at 降順）

### デバウンス方式

トグル操作のいたずら防止・二重カウント防止のため、クライアント側でデバウンスを行う。

1. UIは楽観的に即時反映（タップ直後にアイコン切替）
2. 最後の操作から一定時間（500ms〜1s）経過後に最終状態を送信
3. サーバー側はbooleanフラグのupsert（べき等操作、二重カウント問題なし）
4. 高速トグル中の中間状態は送信しない

### 保存先

有料ユーザー（アカウントあり）のみ。サーバーDB（user_action）に永続化し、パーソナライズに活用。

---

## F-04: アクティビティ履歴（有料）

### 仕様

- ライブラリ画面（S-02）で表示（有料ユーザーのみアクセス可能）
- user_actionのレコード全体を表示（クリック・ブックマーク・メモ等すべての行動履歴）
- 日付ごとにグループ化
- 再訪問可能（リンクをタップで再度開く）

---

## F-05: リンクメモ（有料）

### 仕様

- 各記事に対してメモを残せる（最大1000文字）
- user_action.memo に保存
- メモ追加時にuser_actionレコードが未作成なら新規作成

---

## F-06: 配信モード選択

### 仕様

| モード | コード値 | 説明 |
|--------|---------|------|
| トレンド | trend | 人気・話題の記事を中心に配信 |
| ディープ | deep | 専門性の高い記事を中心に配信 |
| カジュアル | casual | 気軽に読める記事を中心に配信 |
| ディスカバリー | discovery | 普段読まないカテゴリからの記事を混ぜて配信 |
| おまかせ | omakase | 上記をバランスよくミックス（デフォルト） |

- ユーザー設定画面（S-04）で選択
- code_masterテーブルでラベル管理（有効期間付き）
- 設定変更は次回フィード取得時に反映

---

## F-07: カテゴリ設定

### カテゴリ体系

親子階層構造。categoryテーブルのparent_idで管理。seed_categories.pyで初期投入（冪等）。

```
テクノロジー（大分類）
├── Web開発
├── インフラ・DevOps
├── AI・機械学習
├── セキュリティ
└── モバイル開発

ビジネス・経済（大分類）
├── 経営・戦略
├── マーケティング
├── 投資・金融
└── スタートアップ

キャリア（大分類）
├── 転職・就活
├── マネジメント
├── 副業・個人開発
└── 働き方

教養・ライフ（大分類）
├── 書評・読書
├── 歴史・哲学
├── 語学
├── 生産性・習慣
└── マネー

サイエンス（大分類）
├── 物理・数学
├── 生物・医学
└── データサイエンス
```

- 初期データはシステム稼働前に投入（マスタデータ）
- ユーザーはいつでもカテゴリを追加・削除可能

### 配信比率の設計

**カテゴリweight:**
- 大分類間は均等配分（偏りを防止）
- 大分類内の子カテゴリはデフォルト均等割り
- 子カテゴリ未選択の大分類は、大分類全体として配信（子カテゴリ指定なし）
- IPW（逆傾向スコア重み付け）により日次でweight更新（有料ユーザーのみ、**MVP未実装**）
- weight範囲: 0.2〜0.5（マイルドな調整、極端な偏りを防止）

**タグweight（カテゴリより優先）:**
- タグはシステムが自動付与（ソース側メタデータ優先、なければタイトル・要約から自動抽出）
- ユーザーの行動記録（クリック/バウンス/ブックマーク）からタグweightをIPWで日次調整（**MVP未実装**）
- カテゴリより粒度が高く、ユーザーの性向をより正確に反映する
- weight範囲: カテゴリと同様 0.2〜0.5

**スコアリング優先順位:**
タグweight > カテゴリweight > 配信モード

---

## F-08: 週次ランキング

### 仕様

- contentテーブルからbookmark_count降順で直接取得（上位200件）
- カテゴリ別にグループ化して表示（親カテゴリ単位）
- 各カテゴリはデフォルト5件表示、「もっと見る」で5件ずつ追加表示
- 各カテゴリの全件数を表示
- 記事はリンク付き（タイトルクリックで元記事に遷移）、公開日も表示
- weekly_rankingテーブルは将来の週次集計バッチ導入時に使用予定（MVP段階では未使用）
- 過去週のアーカイブ閲覧は将来対応

---

## F-09: アカウント管理

### 概要

無料ユーザーはアカウント不要で利用可能。有料機能（履歴・データ永続化）を利用するにはアカウント登録が必要。

### 認証方式

- カスタムセッション認証（NextAuth互換スキーマ）
- メールアドレス + パスワード認証（bcryptハッシュ）
- セッション管理: DBセッション方式（sessionテーブル + crypto.randomUUIDトークン、サーバー側で即時無効化可能）
- ルート保護: proxy.ts（Next.js 16 proxy convention）でCookie有無によるリダイレクト制御
- OAuth（Google / Apple）は将来対応として併存可能な設計（NextAuth互換スキーマを維持）

### パスワードリセットフロー

1. 認証画面（S-05）で「パスワードを忘れた」リンク押下
2. メールアドレス入力 → verification_tokenテーブルにトークン生成（有効期限: 10分）
3. Resendでリセットリンクをメール送信
4. リンク押下 → トークン検証（期限切れ・使用済みチェック）
5. 検証OK → パスワード変更画面（S-06）へ遷移
6. 新パスワード + パスワード（確認）を入力
7. 変更完了 → 認証画面（S-05）へリダイレクト

### メール送信基盤

- Resend（月3,000通無料枠、MVP期間中は十分）
- パスワードリセットメールのみ（MVP時点）

### クロスプラットフォーム（将来対応）

- Web版でMVP完成後にReact Native（Expo）で展開
- 同一アカウントでWeb版・アプリ版にログイン
- ブックマーク・設定・履歴はすべてサーバー同期

---

## コンテンツ収集

### 収集タイミング（Python別プロセス）

- 日次バッチ: 1日1回、3ステップで処理
  1. **collect_all.py**: 全ソースからRSS/APIで記事を収集し、JSONファイルに出力
  2. **register.py**: 収集済みJSONをDBに登録（URL単位のUPSERT）。ルールベースでカテゴリ分類
  3. **autotag.py**: 未分類記事（category_id IS NULL）をGemini APIでカテゴリ分類
- URL重複時はUPSERT（タイトル・メタ情報を更新、category_idは既存値を優先）
- 有料ユーザーのIPW評価 → タグweight・カテゴリweight更新は**MVP未実装**（将来対応）

### ソース別仕様

#### はてなブックマーク（第1優先）

| エンドポイント | 用途 |
|---------------|------|
| `https://b.hatena.ne.jp/hotentry/{category}.rss` | カテゴリ別ホットエントリ |
| `https://b.hatena.ne.jp/entrylist/{category}.rss` | カテゴリ別新着エントリ |
| `https://b.hatena.ne.jp/entry/jsonlite/?url={url}` | 個別記事のブックマーク情報 |

**はてブカテゴリ:**
`all` / `general` / `social` / `economics` / `life` / `knowledge` / `it` / `entertainment` / `game` / `fun`

#### RSSフィード（第2優先）

- note: `https://note.com/{creator}/rss`
- 個人ブログ・ニュースサイト: 各サイトのRSS/Atomフィード

#### Web検索結果（第3優先・将来）

- Google Custom Search API
- 実装は後回し

### 重複排除

- 記事URLで一意制約（content.url）
- 同一URLが複数ソースから取得された場合、先勝ち（既存レコードがあればスキップ）

---

## データ取得・処理方式

API Route（route.ts）は作成しない。全処理をServer Components / Server Actions / 外部バッチで実装する。

### Server Components（データ取得・画面描画）

Server Componentで直接DBアクセス。認証状態はsession.tsのgetCurrentUser()で判定。

- フィード取得（条件はcookie/URLパラメータから取得。認証不問）
- カテゴリ一覧（階層構造。認証不問）
- アクティビティ履歴一覧（有料・認証必須）
- ユーザー設定取得（有料・認証必須）
- 週次ランキング取得（有料・認証必須）
- 記事に対するユーザーアクション取得（有料・認証必須）

### Server Actions（ミューテーション処理）

- 行動記録（クリック/バウンス/ブックマーク/メモ）— 有料・認証必須
- ユーザー設定更新（配信モード等）— 有料・認証必須
- ユーザーカテゴリ設定更新 — 有料・認証必須
- パスワードリセット申請（メール送信）— 認証不問
- パスワード変更（トークン検証 + 新パスワード）— 認証不問

### 認証（カスタムセッション）

session.ts（lib/session.ts）でセッション管理を実装。getCurrentUser() / requireUser() / createSession() / destroySession() の4関数で認証フローを制御。NextAuthは使用しない。

### 日次バッチ（Python別プロセス）

Next.jsとは独立したPythonスクリプトとして実装。GitHub Actions cronで毎日JST 06:00に自動実行。

1. **collect_all.py**: 全ソース（はてブ・Qiita・Zenn・GIGAZINE）からRSS/API収集 → output_all.json出力
2. **register.py**: JSONからDBへUPSERT登録。ルールベース分類（タグ→ソース→タイトル）+ ドメインbanlistフィルタ
3. **autotag.py**: 未分類記事をGemini 2.5 Flash-LiteでLLMカテゴリ分類

---

## マネタイズ

### プラン

無料 = アカウント登録なし / 有料 = アカウント登録あり

| 機能 | 無料（アカウントなし） | 有料（アカウントあり） |
|------|:----:|:----:|
| フィード表示 | ○ | ○ |
| 条件変更（カテゴリ・配信モード） | 1日1回 | 自由 |
| ブックマーク | × | ○ |
| リンクメモ | × | ○ |
| 配信モード選択 | ○ | ○ |
| カテゴリ設定 | ○ | ○ |
| 週次ランキング | × | ○ |
| アクティビティ履歴 | × | ○ |

### 課金基盤（将来）

- Stripe or RevenueCat（モバイル）
- 初期はフラグ管理のみ（user.planで有料切替）
