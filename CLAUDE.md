# Yaminabe (闇鍋)

## コンセプト
エコーチェンバーと成長を阻むネット環境からの脱却アプリ。
「何が出てくるかわからない」体験を通じて、視野拡大・自己成長・情報収集を両立する。

## コアバリュー
- **発見 × 成長 × 好奇心** — 真面目な自己研鑽と「面白そう」という興味の両立
- **意図的なランダム性** — パーソナライズしつつもエコーチェンバーを回避する
- 暇つぶしと自己研鑽と情報収集を兼ねる

## 主要機能
- リンク/RSSフィードの提示（メイン機能）
- 日次更新（有料ユーザは1日何回でも更新可能）
- ブックマーク機能
- 履歴管理（有料機能）
- リンクのタップ/スキップ行動記録
- 視野の広がりを可視化するダッシュボード（有料機能）
- 温度調節スライダー（0=完全パーソナライズ ↔ MAX=完全ランダム）

## パーソナライズ設計
- 初回セットアップで興味カテゴリを選択（タグベース）
- アプリ内行動（タップ/スキップ）から学習
- ユーザがパラメータ調整可能：実用方面、関連分野の選択、ランダム性の温度

## マネタイズ
- 無料：1日の更新回数制限あり（例：3回）
- 有料：
  - 更新回数無制限
  - 履歴管理
  - 視野可視化ダッシュボード

## コンテンツソース（優先順）
1. はてなブックマーク（公開API・RSS）
2. RSSフィード（note、個人ブログ、ニュースサイト）
3. Web検索結果（Google Custom Search API等）
- X（Twitter）は利用規約・API制限のため対象外

## 技術スタック
- **フロントエンド（Web）**: Next.js (React)
- **フロントエンド（アプリ）**: React Native (Expo)
- **バックエンド/DB**: Supabase (PostgreSQL)
- **認証**: NextAuth (Auth.js) + Prisma Adapter
- **共有ロジック**: Web/アプリ間で共通化できるビジネスロジックは共有パッケージとして切り出す
- **コンテンツ取得**: Next.js API Routes（はてブAPI・RSS取得・パース処理）

## 技術方針
- Web版とネイティブアプリ版を両立（同一機能）
- アカウント認証により同一アカウントでクロスプラットフォームログイン（Supabase Auth）
- monorepo構成（例: Turborepo）でWeb/アプリ/共有パッケージを管理

## MVP スコープ
- 上記主要機能をすべて含む
- まずはコンテンツソース第1優先（はてブ）から実装

---

## AI協業ルール

- **実装前に必ず計画を提示**し、承認を得てから着手する
- **完了条件**を明文化してから着手する
- ファイルの削除・上書き時は変更内容を事前に説明する
- 過剰な抽象化・未使用コードのコミット・ドキュメントの関心分離違反を行わない
- 詳細は `.docs/project-policy.md` を参照

### ハードゲート
計画を提示し「承認」の返答を受け取るまで、コード編集・ファイル作成・コマンド実行を一切行わない。

### サブエージェントにセッション履歴を渡さない
サブエージェント起動時、必要な情報だけを構成して渡す。セッション全体を渡さない。

### レビュー後に完了宣言
作業完了を宣言する前に、検証コマンド（テスト・lint・型チェック等）を実行し結果を提示する。結果なしに「完了」と言わない。

---

## 仕様書

| ドキュメント | パス | 内容 |
|-------------|------|------|
| プロジェクト方針書 | `.docs/project-policy.md` | 基本理念・AI協業ルール・計測・フェーズ管理 |
| セキュリティ対策 | `.docs/secure-dev-rules.md` | 機密漏洩対策・開発環境隔離策 |
| アーキテクチャ仕様書 | `.docs/architecture.md` | OS・ミドルウェア・使用言語・FW・ORM・主要ライブラリ・インフラ構成・認証フロー |
| アプリ仕様書 | `.docs/spec.md` | 画面一覧・UI仕様（モックキャプチャ含む）・処理詳細・API一覧・マネタイズ |
| コーディング規約 | `.docs/coding-style.md` | 命名規則・コーディングスタイル |
| DB定義書 | `.docs/db-schema.md` | テーブル定義・ER図・インデックス |
| シーケンス図 | `.docs/sequence.md` | 主要機能のシーケンス図（認証・コンテンツ取得・行動記録等） |
| スケジュール | `.docs/schedule.md` | フェーズ別マイルストーン・タスク分解 |
| Git運用規約 | `.docs/git-operations.md` | ブランチ戦略・コミット規約・リリースフロー |

---

## 命名規則

- 同一階層のドキュメント・資産はケースを揃える
- `.docs/` 配下: ケバブケース（例: `project-policy.md`）
- `.issue/` `.investigation/` `.user-eval/`: `{連番3桁}_{スネークケース概要}.md`

---

## 課題管理・調査管理

| フォルダ | 用途 |
|---------|------|
| `.issue/` | 課題管理（Status: Open / In Progress / Closed） |
| `.investigation/` | 調査タスク |
| `.user-eval/` | 開発プロセス振り返り・評価 |

**命名規則**: `{連番3桁}_{スネークケース概要}.md`

---

## 開発方針（mmkからの教訓）

- データソース（はてブAPI・RSS）の仕様を実装前に徹底調査する
- .gitignore・環境変数の管理はフェーズ4（ドキュメント整備）で確定
- ESLint/Prettier/tscの厳格設定はフェーズ4で導入、CIで自動チェック
- 課題は発生時に`.issue/``.investigation/`に起票し、ユーザーが方向性確認・軌道修正を行う
